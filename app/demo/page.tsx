import { DemoLayout } from '@/src/components/dashboard/DemoLayout';

export const metadata = {
  title: 'BrightCart Demo — HSBC AI Launchpad',
};

export default function DemoPage() {
  return <DemoLayout />;
}
